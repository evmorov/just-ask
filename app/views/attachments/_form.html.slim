= f.fields_for :attachments, attachable.attachments.new do |attachment|
  = render 'attachments/form_fields', f: attachment
  = link_to_add_association 'Another file', f, :attachments, partial: 'attachments/form_fields'
