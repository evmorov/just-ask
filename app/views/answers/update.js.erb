<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |message| %>
    $('<%= "#edit-answer-error-#{@answer.id}" %>').html('<%= j message %>');
  <% end %>
<% else %>
  $('<%= "#answer_#{@answer.id}" %>').replaceWith('<%= j render @answer %>');
<% end %>
